{
    "$schema": "https://www.schemastore.org/package.json",
    "private": true,
    "type": "module",
    "scripts": {
        "build": "cross-env NODE_OPTIONS=--max-old-space-size=4096 cross-env GOMAXPROCS=1 vite build",
        "build:safe": "cross-env NODE_OPTIONS=--max-old-space-size=4096 cross-env GOMAXPROCS=1 vite build --minify terser",
        "build:esbuild": "cross-env NODE_OPTIONS=--max-old-space-size=4096 cross-env GOMAXPROCS=1 cross-env USE_ESBUILD_MINIFY=true vite build",
        "dev": "vite"
    },
    "devDependencies": {
        "@tailwindcss/vite": "^4.0.0",
        "axios": "^1.11.0",
        "concurrently": "^9.0.1",
        "cross-env": "^10.1.0",
        "laravel-vite-plugin": "^2.0.0",
        "tailwindcss": "^4.0.0",
        "terser": "^5.36.0",
        "vite": "^7.0.7"
    },
    "dependencies": {
        "@vitejs/plugin-vue": "^6.0.1",
        "vue": "^3.5.24"
    }
}
